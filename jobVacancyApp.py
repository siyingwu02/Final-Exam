"""
Implements interaction with the end user and drives the application logic
"""
import jobVacancy
import contractJobVacancy


class JobVacancyApp:
    #create instance use to call the other two class
    jobVacancyObj = jobVacancy.JobVacancy(jobCode, position, mininumQualifications, salaryOffered)
    contractJobVacancyObj = contractJobVacancy.ContractJobVacancy(jobCode, position, mininumQualifications, salaryOffered)
    def run(self):
        # Update the class definition as per the instructions in the PDF
        pass
